import styled from 'styled-components';

const Installers = () => {
  return <InstallersPageTitle>Installers page</InstallersPageTitle>;
};

const InstallersPageTitle = styled.h1`
  text-align: center;
`;

export default Installers;
