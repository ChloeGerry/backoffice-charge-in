import styled from 'styled-components';

const Terminals = () => {
  return <TerminalsPageTitle>Terminals page</TerminalsPageTitle>;
};

const TerminalsPageTitle = styled.h1`
  text-align: center;
`;

export default Terminals;
