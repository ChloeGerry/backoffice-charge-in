import styled from 'styled-components';

const Goals = () => {
  return <GoalsPageTitle>Goals page</GoalsPageTitle>;
};

const GoalsPageTitle = styled.h1`
  text-align: center;
`;

export default Goals;
